pipeline {
  agent any
  stages {
    stage('Fetch Repo') {
      steps {
        echo 'Fetching Repository'
        ansiColor(colorMapName: 'red') {
          echo 'Checking color input'
        }

      }
    }

  }
}